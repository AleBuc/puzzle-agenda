spring:
  application:
    name: puzzle-agenda
  datasource:
    url: jdbc:postgresql://localhost:5432/puzzle_agenda
    username: puzzle_agenda
    password: puzzle_agenda
  flyway:
    locations: classpath:db/migration

server:
  port: 8080
